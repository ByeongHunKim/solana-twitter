# installation

## FE
- `/app` 
    - `$ npm install`

- `$ npm run serve:devnet`

# Program (Rust:: Anchor)
- `/solana-twitters` 
    - `$ npm install`

## after edit
- `$ anchor build`
- `$ anchor deploy`
- `$ anchor run test`
- edit programId as a new one : `Anchor.toml` , `programs/solana-twitter/src/lib.rs`

